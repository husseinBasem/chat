import 'package:flutter/material.dart';

class TryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Color.fromRGBO(42, 42, 42, 100.0),
      child: Center(
        child: Image.asset('images/1721.png'),
      ),
    );
  }
}
